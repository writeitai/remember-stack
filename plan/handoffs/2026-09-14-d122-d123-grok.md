# Grok implementation: document references and contextual fact nomination

The user authorized D122/D123 implementation, Antigravity review and parent final
review/adjustments/merges. The parent owns merges and releases. Keep the solution
simple, YAGNI, with clear human-readable instructions and prompts. Read CLAUDE.md,
D122/D123, D120/D121, their analysis and the lean processing delivery plan.

Use your isolated provided worktree. D119 multi-span extraction is PR #400;
D120/D121 is a separate concurrent lane. Inspect both read-only. Begin useful
mapping, fixtures and independent context-binding work; integrate D119's completed
source-span implementation before changing E2 scheduling. Before final review,
rebase onto completed D119 and D120/D121. Do not cherry-pick uncommitted work, edit
another lane's files or invent a parallel multi-span implementation. Parent will
provide PR/head integration updates in the supervisor note path in your launcher.

Implement the accepted complete contracts: persisted/reused Selection results,
idempotent Selection barrier, bounded preceding exact-source cards, explicit
negative reuse dependencies, zero-proposition no-call completion, D119 full-span
remapping and 500-version same-claim-ID reuse. Implement generic source-backed
normalizer context references, ordinary entity resolution, immutable application
junction bindings with atomic staging, positive current-testimony membership,
canonical-subject exclusion and baseline-preserving additional nomination.

Do not add an event type/table, per-fact event column, new date window, local alias
registry, grouped adjudication, event scheduler or profile neighborhood fetch.
D122 adds no third extraction call. D123 adds no dedicated event extractor,
profile summarizer, or semantic-checker stage; ordinary entity resolution may
still invoke its existing model tier for a newly supplied context reference, and
that cost is measured. Dates remain on supported facts; profile descriptions are
derived. Existing inference-outside-locks and source deletion contracts apply.
Do not drop assertions for auxiliary context overflow. Same event never means
same proposition; D120 remains the semantic rule. Context-empty is not novelty.

Meaningful tests include source-grounded references, changed/competing/zero-to-new
introductions, bounded negative dependencies, offset shifts, ambiguous remapping,
worker-order permutations, barrier restart and empty documents, 500-version reuse,
attribution, relation/observation parity, baseline candidate preservation, support
moves, withdrawal, merge/unmerge, stale snapshots and source erasure during both
extraction and adjudication. Run affected PostgreSQL integration and ordinary CI
lanes, with isolated local DBs; do not reset another lane's database.

Record exact token/candidate coverage evidence without claiming model-semantic
quality from provider doubles. No paid LoCoMo run, remote benchmark changes,
provider-default changes, answering/judging changes or legacy-store conversion.
Never wipe an existing store. Clean-store migration refusal remains explicit.

Create/push a draft PR with checked repository CLA text (authorized), precise
behavior, source contracts, tests and limitations. Rebase to latest main and
regenerate all migration/generation/manifest/protocol artifacts from final source.
Update shipped documentation in the same PR. Do not merge or release yourself.

Obtain review with exactly:

```text
agy --dangerously-skip-permissions --print-timeout 180m0s -p "<review prompt>"
```

Include exact SHA/worktree/diff, binding designs and relevant test evidence.
Request read-only correctness, simplicity, grounding/reuse, ordering/atomicity,
forget, prompt/date clarity and nomination review. Keep the multi-hour timeout.
Address material findings and obtain follow-up review on substantive fixes.

Finish with a durable `plan/implementation_evals/` report and final output naming
PR/head, complete behavior, tests/results, coverage/token measurements, review
SHA/verdict/fixes, remaining risks and integration needs. Parent independently
reviews the code and combined result. Do not stop at a plan when implementation
work remains possible.
