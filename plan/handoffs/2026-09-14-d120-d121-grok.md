# Grok implementation: clear prompts and concise adjudication inputs

The user authorized implementation, Antigravity review, CLA assent, parent final
review/adjustments/merges and appropriate releases. You implement D120/D121 only;
the parent owns integration, merges and releases. Keep code simple, YAGNI, and all
instructions/prompts clear enough for a human to understand without this chat.
There is no separate human prompt-exam requirement.

Read CLAUDE.md, the D120/D121 designs, their analysis and
`plan/plans/lean_processing_delivery.md`. D122/D123 and grouping are outside your
code scope. Do not change locks, candidate/evidence membership, provider defaults,
fact temporal schema or answering/judging. A formatter must not silently summarize
away semantic evidence. Implement an actual closed model response adapter for
short handles; an unchanged UUID schema with informal F1 prose is incorrect.

Work in the isolated branch/worktree provided in your launcher prompt. D119 is
being implemented separately in PR #400 at
`/Users/jpuc/code/moje/ultimate_memory/ugm-multispan-claims`; read-only inspection is
allowed, but never edit it. Start independent adjudicator/normalizer work. Before
final review, fetch main and PR #400 status. Integrate/rebase onto completed D119
when available; avoid overwriting its E2 source-span schema or generation pins.
If #400 remains active, commit your useful independent work and report the exact
remaining integration step rather than blocking all work on it.

Test actual translation/application, stale attempts, identical text from distinct
sources, witness/currency/attribution preservation, support moves, contradictions,
date clearing, source forget and PostgreSQL retries. Include contrasting semantic
fixtures and deterministic token measurements on representative inputs. Mocked
answers do not prove real-model quality. Use isolated local databases and preserve
other processes. Never access/mutate remote v27/v28/v18 stores or launch paid LoCoMo
inference. Do not introduce a legacy conversion framework or reset existing data.

Update affected generations, protocol identities, shipped docs and design details
in the same PR. Rebase onto current main before final review and regenerate pins
from source. Create/push a draft PR against main with a concrete description and
the repository's checked CLA text (user authorized). Do not merge or release.

After implementation and meaningful checks, obtain an independent Antigravity
review using exactly:

```text
agy --dangerously-skip-permissions --print-timeout 180m0s -p "<review prompt>"
```

Do not substitute another agent. Include exact head SHA, worktree/diff, designs,
test evidence and review focus: correctness, prompt clarity, date ambiguity,
semantic information retention, mapping/retry/forget safety and simplicity. The
reviewer must inspect read-only. Preserve its report, address material findings,
and request follow-up review of substantive fixes. Keep the multi-hour timeout.

Write the final implementation report in `plan/implementation_evals/`, naming the
PR/head, actual behavior, field projection map, test commands/results, token
measurements, exact reviewed SHA/verdict, fixes and limitations. Parent reviews
your final diff and combined behavior. Do not end at an implementation plan or
claim completion while required work remains.

## Authorized simplification (no context-only producer)

D123 adds no context-only or opposite-plane fact payload. This lane therefore
does not implement editable flags, a context-only checker, or a mapping
registry. Real handle validation remains: unknown names, wrong kinds, and
unhydrated W-names fail in the translator; a stale reply fails the prepared
attempt's compare-and-swap and fingerprint. A future context-only payload
would need an explicit writer and adapter contract.

D119 PR #400 must be integrated before final E2 prompt/schema, final
generation/protocol pins, and final review/merge. Preserve D119 coherent
claims; do not restore simplest-atomic splitting.
